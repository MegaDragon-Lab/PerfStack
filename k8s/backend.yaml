apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: perfstack
  labels:
    app: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      serviceAccountName: k6-runner-sa
      containers:
        - name: backend
          image: k3d-perfstack-registry:5000/library/perfstack-backend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          env:
            - name: DATA_DIR
              value: "/data"
          volumeMounts:
            - name: backend-data
              mountPath: /data
            - name: docker-sock
              mountPath: /var/run/docker.sock
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
      volumes:
        - name: backend-data
          persistentVolumeClaim:
            claimName: backend-data
        - name: docker-sock
          hostPath:
            path: /var/run/docker.sock
            type: Socket
---
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: perfstack
spec:
  selector:
    app: backend
  ports:
    - port: 8000
      targetPort: 8000
