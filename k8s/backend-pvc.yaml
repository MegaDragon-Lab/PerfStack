apiVersion: v1
kind: PersistentVolume
metadata:
  name: backend-data-pv
spec:
  capacity:
    storage: 100Mi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  hostPath:
    path: /host-data/perfstack
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backend-data
  namespace: perfstack
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  volumeName: backend-data-pv
  resources:
    requests:
      storage: 100Mi
